{
    "name": "highdesert",
    "version": "1.0.0",
    "description": "High Desert theme for Drupal 10.",
    "author": "Dan Ruscoe",
    "license": "MIT",
    "scripts": {
        "build-css": "sass sass:css --no-source-map",
        "watch-css": "sass sass:css --watch --no-source-map",
        "lint-sass": "sass-lint --config .sass-lint.yml sass/*.scss --verbose"
    },
    "dependencies": {
        "sass": "1.62.0",
        "sass-lint": "1.13.1"
    }
}
